// src/concurrency/DeadlockDetector.ts — Phase 5
// Wait-for-graph DFS cycle detector. Runs every DEADLOCK_CHECK_INTERVAL_MS.
export class DeadlockDetector {
  constructor(_lockManager: unknown, _txnManager: unknown) {}
  start(): void { throw new Error('NYI — Phase 5'); }
  stop(): void  { throw new Error('NYI — Phase 5'); }
}
