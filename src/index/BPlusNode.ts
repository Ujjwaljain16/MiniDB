// src/index/BPlusNode.ts — Phase 2
// Page-backed B+ tree node encode/decode (internal + leaf formats).
export class BPlusNode {
  constructor(_buf: Buffer) {
    throw new Error('BPlusNode: not yet implemented — Phase 2');
  }
}
