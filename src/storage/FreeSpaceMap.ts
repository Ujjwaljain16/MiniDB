// src/storage/FreeSpaceMap.ts — Phase 1
// 1-byte-per-page occupancy map stored in page 0 of each heap file.
export class FreeSpaceMap {
  constructor(_bufferPool: unknown, _diskManager: unknown) {
    throw new Error('FreeSpaceMap: not yet implemented — Phase 1');
  }
}
