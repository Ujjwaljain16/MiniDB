// src/storage/Page.ts — Phase 1
// Slotted page layout: 4096-byte Buffer wrapper with insert/delete/compact.
export class Page {
  constructor(_buf: Buffer) {
    throw new Error('Page: not yet implemented — Phase 1');
  }
}
